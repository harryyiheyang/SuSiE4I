% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blockwise_crossprod.R
\name{weighted_crossprod}
\alias{weighted_crossprod}
\title{Row-chunked weighted cross-products}
\usage{
weighted_crossprod(X, w, M = NULL, n_threads = 1L, block_size = 10000L)
}
\arguments{
\item{X}{Numeric matrix.}

\item{w}{Non-negative weight vector of length `nrow(X)`.}

\item{M}{Optional matrix with `nrow(X)` rows; `crossprod(X, M)` is
returned unweighted, so pre-multiply by `w` when needed.}

\item{n_threads}{Number of OpenMP threads for the reference-BLAS path.}

\item{block_size}{Number of rows per chunk.}
}
\value{
A list with `XtWX` and `XtM`.
}
\description{
Computes `crossprod(X, X * w)` and `crossprod(X, M)` in one pass over row
chunks of `X`, without forming the weighted `n x p` copy of `X`.
}
